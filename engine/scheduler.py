# TODO Person 2
# build_dag(jobs: dict) -> adjacency list
# detect_cycles(dag) -> raises error naming the cycle
# topological_sort(dag) -> ordered list of job names
# get_parallel_groups(dag) -> list of lists (jobs that can run together)

def build_dag(jobs: dict):
    raise NotImplementedError

def detect_cycles(dag):
    raise NotImplementedError

def topological_sort(dag):
    raise NotImplementedError