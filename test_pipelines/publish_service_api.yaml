name: publish-service-api
version: 0.1.0
dependencies:
  - name: lib-core
    version: "^1.0.0"
  - name: lib-http
    version: "1.0.0"
jobs:
  build:
    runtime: alpine:3.18
    resources:
      cpu: 0.5
      memory: 256Mi
    steps:
      - name: build
        run: "echo 'service-api built'"
artifacts:
  - name: service-api
    version: 0.1.0
    path: ./out.tar.gz
