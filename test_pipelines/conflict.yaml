name: conflict-pipeline
version: 1.0.0
dependencies:
  - name: lib-core
    version: ">=2.0.0"
  - name: lib-http
    version: "^1.0.0"
jobs:
  build:
    runtime: alpine:3.18
    steps:
      - name: fail-step
        run: "echo 'Should not run because resolution fails first'"
