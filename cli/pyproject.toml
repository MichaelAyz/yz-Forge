[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "forge-cli"
version = "0.1.0"
description = "Forge CI/CD CLI"
dependencies = [
    "click",
    "httpx",
    "pyyaml"
]

[project.scripts]
forge = "forge:cli"
