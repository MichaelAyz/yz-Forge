# TODO Person 1
# init_db() -> creates tables if not exists
# save_artifact(name, version, sha256, size, deps, publisher) -> None
# get_artifact(name, version) -> dict or None
# list_versions(name) -> list of version strings
# artifact_exists(name, version) -> bool

def init_db():
    raise NotImplementedError